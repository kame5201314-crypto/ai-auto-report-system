services:
  - type: web
    name: ai-auto-report-system
    runtime: docker
    region: singapore
    plan: free
    rootDir: backend
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DEBUG
        value: "false"
      - key: CORS_ORIGINS
        value: "https://dealer-management-system-teal.vercel.app,http://localhost:5173"
    healthCheckPath: /api/health
